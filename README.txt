Pedro Mérida 201773583-K

Instrucciones para la ejecucion:

Se debe estar posicionado en la carpeta donde este el archivo "etp.c", luego de eso ejecutar
el comando "make", cuando ya todo se haya compilado ejecutar el comando "./etp"

Consideraciones:

Si se quiere probar multiples archivos, se debe ejecutar el codigo nuevamente para cada archivo.
